// Entry point para o painel administrativo
import './app.js';

// Expor globalmente para compatibilidade com HTML inline handlers
if (typeof window !== 'undefined') {
    console.log('✓ Admin bundle carregado');
}
