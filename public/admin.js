/**
 * ORQUESTRADOR PRINCIPAL - PAINEL ADMINISTRATIVO
 * 
 * Este arquivo é apenas um ponto de entrada leve que:
 * - Importa e inicializa os módulos
 * - Expõe controllers globalmente para o HTML
 * - Gerencia o ciclo de vida da aplicação
 * 
 * Toda a lógica está separada em módulos especializados.
 */

import App from './js/app.js';

// A aplicação é inicializada automaticamente pelo app.js
// quando o DOM estiver pronto.

console.log('✅ Orquestrador carregado - Aplicação modular iniciada');
